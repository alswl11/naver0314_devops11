import React from 'react';
import {Alert} from "@mui/material";

const ThreeApp = () => {
    return (
        <div>
            <Alert>ThreeApp - 데이터 추가, 삭제, 출력(부모 자식 컴포넌트)</Alert>

        </div>
    );
};

export default ThreeApp;